<br>
<div class="row">
<div class="col-md-4">
  <%= bootstrap_form_with(model: [ @post, @comment], local: true) do |form| %>
      <p>
        <%= form.text_area :comment_message, rows:1 %>
        <%= form.submit "Post Comment", class: "btn btn-secondary btn-sm" %>
      </p>
  <% end %>
</div>
</div>
